type Company {
  id: ID!
  name: String!
  description: String
}

type Job {
  id: ID!
  date: String!
  title: String!
  description: String
  company: Company        #Object Association
}

type Query {
  jobs: [Job!]
}
